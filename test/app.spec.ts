describe('AppModule', () => {
  it('should be defined', () => {
    expect(true).toBe(true);
  });

  it('should have basic functionality', () => {
    const result = 1 + 1;
    expect(result).toBe(2);
  });
});
